"use strict";

const rules = require("./rules");

describe("After the battle-results for all battles have been calculated, setArmyStrength is used to adapt the army-value for each of the oppnents.", () => {
  it("self check", () => {
    expect(1).toBe(1);
  });
});
